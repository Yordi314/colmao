{
  "name": "colmao-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "cd backend && npx prisma migrate deploy && npx tsx prisma/seed.ts && npx tsx prisma/enrich-demo-data.ts && npm start"
  }
}
